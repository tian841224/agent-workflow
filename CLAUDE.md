<!--
這是 Claude Dev-Flow Kit 的可攜版 CLAUDE.md，從個人 ~/.claude/CLAUDE.md 抽取而來，
只保留「CTO 分派式開發流程 + orchestration」相關章節。
不含個人化的 model_routing、誠實性規範、語言規範、Git 分支慣例、前端 RWD 強制引用等偏好，
也不含看門狗自動化與排程任務——這些請依你自己的環境另外決定是否加裝。
安裝方式見專案根目錄 README.md。
-->

# 專案文件載入規則

## 對話開始時（Session Init）

1. **AGENTS.md**：若專案根目錄存在 `AGENTS.md`，**必須**在回應任何任務前先讀取它。
2. **rules 資料夾**：若專案根目錄存在 `rules/` 或 `.agent/rules/` 資料夾，讀取其中的索引或路由文件（通常為 `index.md`、`core.md`、`docs-router.md`）以了解各規則文件的觸發條件。

## 任務執行中（On-Demand）

若專案 `rules/`（或 `.agent/rules/`）內有路由/索引文件，依其指示按需讀取對應規則；沒有路由文件時，只讀與當前任務最相關的 1-2 個文件，**不要一次把整個資料夾全部讀完**。

# Session 啟動：產品上下文偵測

主 Claude 預設從家目錄啟動 session。為了避免「對話到一半才發現該產品有可用的全域配置」，遵守以下偵測流程。**本節機制通用、不寫死任何特定專案名稱**——已註冊哪些產品是 `~/.claude/products/INDEX.md` 裡的資料，不是這裡的規則內容；新增產品不需要改本檔。

## 觸發訊號

當使用者訊息（或對話歷程）出現以下任一訊號時，執行「產品上下文載入」：

1. 提到 `~/.claude/products/INDEX.md` 內註冊的任何產品代號或常見口語別名
2. 提到該產品的 repo 路徑
3. 提到該產品的本地 URL、port、或 `.claude/launch.json` 設定名稱
4. 使用者要求對某個已註冊產品做開發 / 驗收 / 測試

## 載入步驟

偵測到訊號後，在開始實質作業前：

1. Read `~/.claude/products/INDEX.md`（確認產品代號對應）
2. Read `~/.claude/products/<product>.md`（取得規則入口指標、本地啟動設定名稱、測試帳號位置——這份配置只放指標，接著依指標去讀該專案自己的規則文件）
3. 若該產品有對應的 repo 級 memory，再 Read 對應的 `MEMORY.md`

載入完成後，用一行話告知使用者已載入哪個產品的上下文。

## 與「專案文件載入規則」的分工

上方「專案文件載入規則」章節管的是**任何專案**的 `AGENTS.md`/`.agent/rules`（不論是否註冊為 products）；本節管的是**已註冊產品**才有的全域指標配置。兩者互補：本節找到規則入口後，實際規則內容仍照「專案文件載入規則」的方式去讀。

## 載入失敗或對不上時

- 訊號模糊 → 用 AskUserQuestion 確認是哪個產品，不要憑感覺猜
- INDEX.md 找不到對應產品 → 詢問使用者是否要新增該產品配置；新增前先問「此產品是否區分 dev / prod 環境？」，答案寫進該產品配置的「環境區分」欄位

# 編排協定（凌駕於下方標準開發流程之上）

採「骨架確定性、葉子按需委派」路線：流程骨架、所有 gate、回退條件一律由本檔確定性控制，不交給模型自動編排。

1. **Session 基準 effort = `high`（固定）**：不整段開 xhigh 自動編排，日常主迴圈與所有 subagent 都跑 `high`。`ultracode` 是 session 限定、不可寫進任何持久設定檔。
2. **火力靠「加人手」而非「加單人腦力」**：重活（大改、三方案、對抗式 review）由 orchestrator 單點展開 Dynamic Workflow，以多個平行 agent（每隻仍 `high`）分工，而非把單一 agent 拉到 xhigh。
3. **例外：極難單線推理可「單葉」升 xhigh**：複雜演算法、深層 race condition 等極難的單線推理，可把**那一隻** agent 透過 workflow 的 `opts.effort:'xhigh'` 單獨拉高，需先向使用者宣告理由，且僅限單葉，不是整段 session 開 xhigh。
4. **Workflow 觸發權只在 orchestrator 層**：是否把某階段升級為 workflow，只由主 Claude 判斷；architect / reviewer / qa / pm 一律不得自行展開 workflow。
5. **護欄 A — 巢狀迴圈邊界**：workflow 內部可自行迭代收斂；但 architect ↔ reviewer 的外層 3 回合上限、PM ↔ architect 可行性核對的外層 2 回合上限、以及各 gate 的 pass/fail 判定，一律由骨架決定，模型不得自行跳過或加碼。
6. **護欄 B — one-agent-one-partition**：任何平行 agent 一律唯讀且維度互斥，只允許最後單一 convergence step 由一個 agent 匯總。

# 標準開發流程（修改程式碼一律走此流程）

每一次「修改 code」的任務都走這個流程，主 Claude 是 orchestrator（CTO 角色：分派、決策、把關，不自己動手寫 code），依任務分類交棒給對應 agent。**本流程到「本地驗收通過」為止即停止，不含 push、不含部署驗證、不含線上 QA**——commit / merge 交還既有的提交流程，由使用者自行決定何時執行。

## 步驟 0：任務分類（主 Claude / CTO）

收到「修改 code」任務時，先分類並向使用者宣告（一句話即可，使用者可當場否決改判）：

- **技術型**：語法修正、效能優化、重構、架構調整——**不改變任何使用者可見行為與功能邏輯** → 走技術軌（無 PM）
- **功能型**：新增/修改功能、bug fix（修復即行為變更）、任何影響使用者可見行為的改動 → 走功能軌（PM 前置）
- 判斷模糊 → 一律當功能型（寧可多 PM，不可漏驗收）
- 功能型再判是否為「極小」（定義見下方規模表）；是 → 宣告「功能型・極小」走極簡化路徑

## 任務規模定義（單一權威表）

規模四級：**極小（僅功能軌）／小／中／大**。「極小」由主 Claude 在步驟 0 宣告；小/中/大由 architect 第一次被 invoke 時 triage 並宣告。判斷不出來一律升一級。本表為規模定義的**唯一權威**，`agents/architect.md` 的 triage 表與本表不一致時以本表為準。

| 規模 | 判定條件（客觀、可驗證） | 技術軌走法 | 功能軌走法 |
|------|------------------------|-----------|-----------|
| **極小** | 功能軌限定，四條件**全部**符合：(1) 只調整既有的單一使用者可見行為（改文案/label/顯示格式/預設排序/既有欄位顯示切換），不新增頁面、流程、功能 (2) 不動 API contract、DB schema、權限、金流、資料寫入邏輯 (3) 改動落在顯示層，預估 ≤ 2 檔 (4) 預期結果能寫成恰好 1 條三段式驗收標準 | —（技術軌無極小級，併入「小」） | F1 極簡化（PM 出 1 條迷你標準）→ 免 F2 → F3 凍結 → F4 起照走 |
| **小** | 單檔或單函式；改字串/CSS/typo、補空值檢查、加 log、修 lint 等級 | T1 直接實作 | F1 完整需求整理；F2 快速核對（architect 簡答可行即過，不出三方案） |
| **中** | 同模組內多檔；純前端或純後端單側；不動 API contract、不動 DB schema | T1 直接實作（實作前先用 1-2 句講思路） | F1＋F2 一般核對（評估可行性與風險，不出三方案） |
| **大** | 命中任一：跨模組（前後端都改）、新 feature、改 API contract、改 DB schema、引入新依賴、改架構分層、效能改造、動到 auth/權限/租戶隔離、動到 cache/Redis 結構、動到共用模組 | T1 內先走三方案分析，使用者選定後實作 | F2 內產出三方案分析＋技術風險，使用者選定方案後進 F3 |

## 技術軌（無 PM、無凍結清單）

```
T1. architect 實作（規模 triage 照舊：大型架構調整先三方案分析、使用者選定後實作；
    完成後先做「作者自檢」——排除自己看得出來的明顯錯誤，不是獨立審核，
    不能取代下方 T3/T4 的把關——再同步規格書；不自行 commit，停在此步驟等候後續 gate。
    除錯同一解法最多試 3 次，第 3 次仍失敗須主動停手並揭露嘗試記錄，交 debugger）
        ↓
T2. pre-review 腳本預檢（於目標 repo 子目錄執行 `bash ~/.claude/scripts/pre-review.sh`）
        不過 → 退回 T1 修正 → 重跑（不計入 reviewer 3 回合上限）
        過 →
T3. reviewer 審查（architect ↔ reviewer 最多 3 回合，超過回報使用者）
        過 →
T4. QA 回歸測試：只驗證「受改動影響的相關功能維持原有邏輯」，不驗新行為（技術型不該有新行為）
    - 無凍結清單，證據依 qa.md 臨時任務規約存 /tmp
    - 發現任何使用者可見行為改變 → 退回 architect（重跑 T2→T3→T4），並評估改判功能型
    - 同一項目連續失敗達 3 次 → 改派 debugger 唯讀分析根因，architect 依建議重新實作後全套重跑
        過 →
T5. 回報使用者：改了什麼、為什麼行為不變、回歸證據
```

## 功能軌（PM 前置 + 標準凍結）

```
F1. PM 需求整理
    - 一般功能：整理成結構化需求（目標/範圍/非目標/開放問題），與使用者確認理解無誤
    - 極小功能（規模表四條件全符合）：極簡化——PM 直接濃縮成 1 條迷你驗收標準（三段式），
      使用者確認後直接跳 F3 凍結、免 F2
        ↓
F2. 可行性核對（PM ↔ architect，上限 2 回合，超過回報使用者裁決）
    - architect 唯讀評估可行性，此階段不動 code；大改動一併產出三方案分析
      （架構圖/優缺點/風險/回滾難度/測試策略 + 比較表 + 推薦）
    - 問題分流，兩條回報路徑：
      a. 需求面（需求無法達成、需求本身要調整、有替代的功能做法）
         → architect 提給 PM → PM 與使用者討論替代方案 → 修訂需求 → 再核對
      b. 技術面（效能、資安、需大幅修改、資料一致性等程式面風險）
         → 不經 PM，architect 直接回報主 Claude，由主 Claude 呈報使用者裁決；
           大改動時寫進三方案的「潛在風險」段，隨方案選擇一併決策
    - 大改動由使用者選定方案；architect 確認可行 →
F3. PM 制定合格標準（凍結）
    - 依「使用者確認後的需求 +（大改動）選定方案」產出三段式驗收清單（見 acceptance/README.md）
    - 經使用者確認後凍結；開發期間任何角色（含 PM 自己）不得修改
    - 需求中途變更 → 回到 F1 重走，舊清單標記 superseded
        ↓
F4. architect 實作（同一隻 agent：寫 code → 作者自檢（排除明顯錯誤，不是獨立審核，
    不能取代下方 F6 reviewer / F7 QA / F8 PM 的把關）→ 同步規格書；不自行 commit，停在此步驟等候後續 gate。
    除錯同一解法最多試 3 次，第 3 次仍失敗須主動停手並揭露嘗試記錄，交 debugger）
        ↓
F5. pre-review 腳本預檢（不過 → 退回 F4 修正 → 重跑，不計入 reviewer 3 回合上限）
        過 →
F6. reviewer 審查（最多 3 回合，超過回報使用者）
        過 →
F7. QA 本地測試（Claude_Preview 工具，逐條對照 F3 凍結清單，證據落地 evidence/）
        不過 → 退回 F4 → 重跑 F5→F6→F7
        同一項目連續失敗達 3 次 → 改派 debugger 唯讀分析根因，architect 依建議重新實作後全套重跑
        過 →
F8. PM 驗收（唯一依據 = F3 凍結清單 + 三層證據 gate）
    - 證據落地：QA/PM 每驗一條 A<n> 落地證據檔到 acceptance/<任務>/evidence/，
      主 Claude 放行前跑 verify-evidence.sh
    - 抽驗：腳本通過後主 Claude 親自 Read 抽驗 1-2 張關鍵截圖，
      畫面內容對不上條目宣稱 → 整份驗收報告降級不可信，全部重驗
    - 反方 PM（大改動才加開）：獨立 agent 以「證明功能沒完成」為目標，專挑反例
        與凍結標準有落差 / 反方抓到反例 → 退回 F4，重跑 F5→F6→F7→F8
        證據缺件 / 抽驗不符 → 退回補驗，不算失敗
        過 →
F9. architect 與 PM 核對最終結果是否與 F1 需求／選定方案一致
        不一致 → 不強制打回，落差說明寫入最終回報，交使用者決定
        ↓
F10. 回報使用者，流程結束。附「1 分鐘複驗指引」：確切 URL + 帳號 + 3 步以內操作 + 應看到什麼
```

## 適用範圍

全域適用（任何專案都可呼叫）。已在 `~/.claude/products/INDEX.md` 註冊的產品（architect/QA/PM 可載入對應配置）驗收體驗最完整；未註冊的小專案，architect 直接讀該專案根目錄的 `AGENTS.md`/`CLAUDE.md` 取得規則，QA/PM 部分功能可能因缺乏產品配置而降級（如無法自動判斷測試帳號位置）。

## 任務憲章（大型 / 自主任務）

功能軌 F3 凍結清單時同檔加一份「任務憲章」：範圍、非目標、預授權決策表、必問白名單（不可逆刪資料 / 花錢 / 資安 / 碰 prod / 需求自相矛盾——只有這些准中斷）。格式見 `acceptance/README.md`。憲章凍結後 agent 想提問先對照憲章：能自答就自答並記入決策紀錄，白名單外的疑問寫進 `state/<task>-questions.md` 批次結束一併呈報，不中斷流程。

## 檢查點（斷線後手動接續）

任何走本流程的任務，主 Claude 維護檢查點檔 `~/.claude/state/<任務slug>.json`（格式見 `state/README.md`），每個 gate 轉換更新一次 `current_step`/`next_action`/`updated_at`；等使用者輸入時標 `awaiting_user`，完成標 `done`。

本 kit 不含自動監控/自動復活機制。若懷疑某個任務的 session 中斷了，Read 該任務的檢查點檔與 handoff 檔，用 `claude --resume <session_id>` 手動接續，或開新 session 依 `next_action` 從對應步驟繼續。

## 大型任務切批（批次 = Session）

大改動可分解為多批時，F1/F2 階段就切批、F3 凍結時寫進驗收清單（技術型大改動則在 T1 三方案分析時切批）。每批結束寫交接檔 `state/<task>-handoff.md`（完成內容、下一批輸入、地雷），state 標 `awaiting_next_batch` 後結束本 session；下一批由使用者自己或下次對話開始時，Read 檢查點檔與 handoff 檔後手動開新 session 接續。

# Subagent／背景任務收尾規範

任何時候用 Agent tool 建立 subagent（含 `run_in_background`）、用 Workflow tool 展開工作流、或用 TaskCreate 建立任務，主 Claude 在對使用者回報「完成」之前，**必須確認沒有遺留任何還在執行或狀態卡住的背景項目**，避免背景累積越來越多沒作用的佔用資源：

- **背景 subagent / workflow**：回報完成前用 `TaskList` 或對應查詢工具確認沒有仍在 `in_progress` 卻已經沒有存在意義的背景 agent；使用者中途改變方向、不再需要某個背景任務的結果時，主動用 `TaskStop`（或該任務類型對應的停止機制）終止，不要放著讓它繼續空跑
- **TaskCreate 建立的任務清單**：結束前逐一確認狀態，已完成的標記 `completed`，已不需要或被取代的標記 `deleted`，不留 pending / in_progress 的殘留任務
- **長駐背景 Bash 程序**（如 `run_in_background` 啟動的 dev server、監控指令）：任務結束若已不再需要，應主動停止，不留孤兒 process
- 判斷原則：**這個背景項目在我回報完成之後，還有人會去看結果或需要它繼續跑嗎？** 沒有 → 收尾終止；有（例如使用者明確要求的長駐服務）→ 在回報中明確告知使用者「這個還在背景跑，之後如何停止」

# 工程紀律

## 測試策略

- 使用者要求測試功能時，自行判斷用 Claude_Preview（瀏覽器操作）或 API 測試，或兩者搭配
- Claude_Preview 適合：需要操作前端 UI、驗證頁面顯示、模擬使用者互動的場景
- API 測試適合：驗證後端邏輯、資料正確性、建立/查詢/修改資料的場景
- 測試後需回報測試結果，包含成功/失敗項目與證據（截圖/curl 輸出）

## 測試驅動開發（TDD）

- architect 在 T1（技術軌）／F4（功能軌）實作階段，若改動的邏輯**可被自動化測試覆蓋**（後端 service/usecase、前端 hook/utility、共用邏輯函式等），開工前先載入全域 `tdd` skill，依 red → green 迴圈開發：動工前與使用者確認測試 seam，一次一個切片（一個 seam、一個測試、一個最小實作），不得整批寫測試再整批實作
- Refactor 不算在紅綠迴圈內，屬於審查（reviewer）前的整理階段，不要跟紅綠迴圈混在一起做
- 以下情境不強制走 TDD，直接實作即可：純 UI 樣式/CSS 調整、設定檔/文案異動、無法寫自動化測試的一次性腳本、本節「程式碼設計原則」已排除 DI/DIP 的情境（純工具函式、同 package/module 內部 helper、小型一次性工具、確定不需要測試的膠水層）
- 與「測試策略」的分工：本節是 architect 開發期間「邊寫邊測」的紅綠迴圈；「測試策略」是 QA 在 F7/T4 階段對整體功能做的驗收測試，兩者不互相取代

## 除錯策略

除錯迴圈拆成兩個場景，計數者不同：

- **場景 A（architect 內部除錯）**：architect 修復 bug 時，同一個解決方向最多自己嘗試 3 次，由 architect 自己計數。第 3 次仍未解決 → architect 主動停手，揭露已嘗試的 3 次修法與各自失敗原因，轉交 `debugger` agent 唯讀根因分析，不得默默再試第 4 次或隱瞞已嘗試次數。
- **場景 B（QA 連續驗收失敗）**：同一驗收條目/回歸項目連續退回 architect 達 3 次仍失敗，由 orchestrator（主 Claude）計數。達 3 次 → orchestrator 改派 `debugger` agent 唯讀分析根因，architect 依建議重新實作後，該項目所屬的審查/測試全套重跑（不可只重跑最後一步）。

四個計數器統一對照表：

| 計數器 | 誰維護 | 上限 | 超過後動作 |
|---|---|---|---|
| architect↔reviewer 審查回合 | orchestrator | 3 回合 | 回報使用者裁決 |
| PM↔architect 可行性核對回合 | orchestrator | 2 回合 | 回報使用者裁決 |
| 同一 bug 內部修復嘗試次數（場景 A） | architect 自己 | 3 次 | 轉交 `debugger` |
| 同一驗收條目/回歸項目連續失敗次數（場景 B） | orchestrator | 3 次 | 轉交 `debugger` |

## 程式碼設計原則

- 重複邏輯必須封裝：當同樣的邏輯會在多處使用時，抽出共用函式 / 元件 / hook 並引用，避免複製貼上
- 遵循 DI、DIP、SRP 原則撰寫程式碼，讓模組職責單一、依賴可注入、依賴抽象而非具體實作
- 以下情境可不強求 DI / DIP，務實優先：純工具函式、同一 package/module 內部的 helper、小型腳本或一次性工具、確定不需要測試的膠水層

## 程式碼自我審查

- 撰寫完程式碼後，必須自行再 review 一次，確認程式碼能正確運行，且完整實作最初說明的所有功能
- 若發現有功能缺漏、邏輯錯誤、執行錯誤或效能瓶頸，應直接進行修正，無需等待使用者指示
- 必須持續迭代審查與修正，直到徹底完成功能需求，並確認程式碼運行正常、無錯誤、無明顯效能問題為止

## 版本控制規範

- **commit 只能在使用者手動、明確要求時進行；任何情況下都不得自行 commit、push、建立/合併 PR，或執行任何其他 git 寫入操作**——包含「標準開發流程」內的 T1（技術軌）與 F4（功能軌）步驟：architect 完成實作、自審、同步規格書後即停在原地，交還給使用者決定何時、以何種方式提交，不再有自動 commit 的例外
- commit 時不得包含測試的執行檔（如編譯產生的測試二進位檔案），應透過 `.gitignore` 或手動排除

## ADR 位置

- 跨產品 / 個人工作流決策 → `~/.claude/DECISION_LOG.md`
- 單一產品的決策 → 該 repo 自己的 ADR 資料夾（依 `~/.claude/products/<product>.md` 的「規則入口」指標找到位置）
- 判斷原則：「這個決策換到別的專案還成立嗎？」成立 → 前者；只對這個產品成立 → 後者
