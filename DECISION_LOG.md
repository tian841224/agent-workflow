# 決策紀錄制度（ADR — Architecture Decision Record，僅限跨產品 / 個人工作流決策）

> 這份文件是「跨產品決策紀錄」這套制度的單一真相來源。主 Claude、architect、以及使用者本人都依這份運作。
> 目的：把每一次「為什麼這樣決定」留存下來，供未來的你我（不論人或 Claude）回頭查證，避免「後人不知道前人為何這樣寫」。

## 一、核心模式：混合存放

決策分兩類，存放位置不同：

| 決策類型 | 存放位置 | 範例 |
|---|---|---|
| **產品相關決策**（綁某個 repo / 產品） | 該 repo 自己的 ADR 資料夾，**不放這裡** | 「某功能改用 XX 狀態機」「某模組改用 weighted round-robin」 |
| **跨產品 / 個人工作流決策**（不屬任何單一產品） | 本檔 `~/.claude/DECISION_LOG.md` | 「所有專案一律用 worktree 模式開發」「products 配置只放指標不重複內容的設計原則」 |

判斷原則：**「這個決策換到別的專案還成立嗎？」** 成立 → 記在本檔；只對這個產品成立 → 記在該 repo 自己的 ADR 資料夾，本檔不重複。

## 二、什麼時候寫（觸發門檻）

走「決策當下立即記」：做出以下任一種**跨產品 / 工作流**決策時，當下就在本檔補一筆。

會觸發（值得記）：
- 跨專案通用的開發流程 / 工具鏈決策（例如標準開發流程本身的設計，或未來對它的重大調整）
- 跨產品都適用的架構慣例（例如 products 配置的設計原則）
- 影響多個專案的重大取捨、回滾成本高或不可逆的個人工作流決定
- 推翻先前的跨產品決策（新條目註明 supersedes，並把舊條目狀態改 Superseded）

不需要寫：只影響單一產品的決策（去該產品自己的 ADR）、一般 bug 修復、小重構、命名/樣式微調。

模糊時：**「半年後我會希望有紀錄這是為什麼嗎？而且不只對一個專案有用？」** 兩者都成立 → 寫在本檔。

## 三、條目範本

```markdown
## DL-NNNN: <一句話標題>

- 日期：YYYY-MM-DD
- 狀態：Proposed | Accepted | Superseded by DL-NNNN | Deprecated

### 背景與需求
（什麼情境、什麼需求或限制逼出這個決策？）

### 決策
（最後決定怎麼做，一句話能講完最好。）

### 考慮過的替代方案
1. 方案 A —— 否決原因 / 取捨
2. 方案 B —— 否決原因 / 取捨

### 後果與風險
- 好處：
- 壞處 / 技術債：
- 回滾難度：低 / 中 / 高（為什麼）

### 適用範圍
（哪些專案適用；未來新專案是否自動適用）
```

## 四、誰負責寫

- 使用者口頭拍板的跨產品決策 → 主 Claude 代寫，補在本檔
- 走標準開發流程時若架構師判斷本次決策屬跨產品性質 → 在回報中提出，由主 Claude 補一筆

---

## 決策條目

（尚未累積任何決策，第一筆從 DL-0001 開始）
